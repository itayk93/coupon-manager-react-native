# שינויים 18.08.2026 — צ'יפים לפי חברות + תיקון Face ID

## מה בוצע

### 1. החלפת כפתורי הקטגוריות בכפתורי חברות
במסך הקופונים (`src/screens/coupons/CouponsListScreen.tsx`) הוחלפה שורת הצ'יפים של הקטגוריות
(משלוחים / סופר / פארם / שוברים) בשורת צ'יפים של החברות בפועל.

כל צ'יפ מציג:
- לוגו של החברה
- שם החברה
- מספר הקופונים של אותה חברה

סדר החברות:
- משמאל לימין, מהחברה עם הכי מעט קופונים ועד החברה עם הכי הרבה קופונים.
- החברה עם הכי הרבה קופונים מופיעה בקצה הימני.

כיוון הגלילה:
- השורה מוגדרת `flexDirection: "row"` (לא `row-reverse`), כך שהקצה הימני הוא החברה
  עם הכי הרבה קופונים.
- בעת טעינת הנתונים מתבצע `scrollToEnd` כדי שהמסך ייפתח על החברה עם הכי הרבה קופונים.

הפילטור:
- לחיצה על צ'יפ של חברה מסננת את רשימת הקופונים לפי אותה חברה.
- הצ'יפ "הכל" מחזיר את כל הקופונים.
- לחיצה חוזרת על אותה חברה מבטלת את הסינון.

### 2. תיקון הפעלת Face ID כשהטלפון נעול
בקומפוננטת `BiometricGate` (`src/components/layout/BiometricGate.tsx`) היה מצב שבו
Face ID הופעל מיד כשהאפליקציה נכנסה לרקע, גם אם הטלפון עדיין נעול.

התיקון:
- נוספה בדיקת `AppState` שמזהה אם האפליקציה באמת פעילה (`active`).
- בקשת הזיהוי הביומטרי תופעל רק כשהאפליקציה חוזרת לחזית.
- הנעילה עדיין מתבצעת כשהאפליקציה יוצאת לרקע, אך לא נפתח דיאלוג Face ID עד שהמשתמש
  פותח שוב את האפליקציה.

## קבצים ששונו

- `src/screens/coupons/CouponsListScreen.tsx`
- `src/components/layout/BiometricGate.tsx`

## בדיקות

- `npm run typecheck` עבר בהצלחה.
- `npm test` עבר בהצלחה (10/10 טסטים).
