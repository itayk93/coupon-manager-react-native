# Copy to .env and fill in. Never commit the filled-in file.
#
# Public app configuration. Values with EXPO_PUBLIC_ are bundled into the app;
# never treat them as secrets.

# --- Google Sign-In public OAuth client identifiers ---
EXPO_PUBLIC_GOOGLE_AUTH_WEB_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_AUTH_IOS_CLIENT_ID=

# --- Supabase project the app talks to ---
EXPO_PUBLIC_SUPABASE_URL=
# Anon key. Public by design — every table is protected by RLS, and the RPC
# surface is revoked down to service_role (supabase/migrations/0016_*).
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# --- Coupon-code encryption ---
# Legacy compatibility only. This value is bundled into mobile builds and is
# not a security boundary. Never reuse it for unrelated secrets.

# --- Local automation email fallback (ignored local file: .env.brevo.local) ---
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=
MULTIPASS_SUMMARY_EMAIL_TO=
