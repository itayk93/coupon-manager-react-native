# סקירת אבטחה — קופון מאסטר

תאריך: 17.08.2026
נבדק מול: קוד המאגר (`main`) + פרויקט Supabase `MaCoupon` (`dugjsiyenazpsoiyduuz`)

מסמך זה עובר על 20 סעיפי אבטחה סטנדרטיים לפני השקה, וקובע לכל אחד: **יש / אין / לא רלוונטי**, ומה לעשות.

## הקשר חשוב

רשימת 20 הסעיפים כתובה לאפליקציית ווב קלאסית עם שרת משלה, סשנים ועוגיות. קופון מאסטר בנויה אחרת: אפליקציית מובייל (Expo/React Native) מול Supabase, כשהאבטחה נשענת על **RLS ברמת מסד הנתונים** ועל Edge Functions. לכן חלק מהסעיפים לא רלוונטיים — לא מתוך הזנחה, אלא כי רכיב המערכת הזה לא קיים.

---

## טבלת סיכום

| # | סעיף | מצב | פעולה |
|---|------|-----|-------|
| 1 | HSTS | ✅ יש | — |
| 2 | CSRF tokens | ⚪ לא רלוונטי | — |
| 3 | איפוס סשן בשינוי סיסמה | ⚪ מטופל בספק | — |
| 4 | תפוגת קישורי איפוס | ✅ יש | — |
| 5 | מניעת זיהוי משתמשים | ✅ יש | — |
| 6 | הגבלת סוגי קבצים | 🟡 חלקי | תוקן חלקית |
| 7 | אימות webhooks של תשלום | ⚪ לא רלוונטי | — |
| 8 | מחירים בצד שרת | ⚪ לא רלוונטי | — |
| 9 | חסימת prompt injection | 🟡 חשיפה קיימת, נזק נמוך | מעקב |
| 10 | תקרת שימוש ב-AI | ✅ **תוקן** | — |
| 11 | הגבלת גודל בקשה | ✅ **תוקן** | — |
| 12 | הגבלת קצב לאיפוס סיסמה | ⚪ מטופל בספק | — |
| 13 | ניקוי קלט לפני שמירה | ✅ יש | — |
| 14 | הידוק CORS | 🟡 פתוח | החלטה נדרשת |
| 15 | חסימת דפדוף תיקיות | ⚪ לא רלוונטי | — |
| 16 | הסרת נתיבי אדמין | ✅ יש | — |
| 17 | נעילת חשבון אחרי כשלים | ✅ **תוקן** | להריץ מיגרציה |
| 18 | לוג אירועי אבטחה | 🟡 חלקי | — |
| 19 | דגלי עוגיות מאובטחים | ⚪ לא רלוונטי | — |
| 20 | הרשאות מסד נתונים | 🔴 **חורים פתוחים** | **דורש טיפול** |

---

## 🔴 פתוח — דורש טיפול לפני השקה

### 20. הרשאות מסד נתונים

**א. פונקציות RPC נגישות למשתמש אנונימי.** 9 פונקציות `SECURITY DEFINER` ניתנות להרצה על ידי `anon` דרך `/rest/v1/rpc/`. שתיים מהן מקבלות מזהה משתמש כפרמטר:

- `get_widget_coupons(p_user_id integer)`
- `get_widget_statistics(p_user_id integer)`

`SECURITY DEFINER` אומר שהפונקציה רצה בהרשאות מי שיצר אותה ועוקפת RLS. אם אין בתוכן בדיקה שהקורא הוא בעל המזהה, כל אחד באינטרנט יכול לשלוף קופונים וסטטיסטיקות של כל משתמש לפי מספר רץ.

> **סטטוס: לא אומת ולא תוקן.** ניסיתי לקרוא את גוף הפונקציות ונחסמתי — קריאות ה-SQL ל-Supabase דורשות אישור הרשאה שלא ניתן. הפונקציות האלה כנראה משרתות את הווידג'ט של iOS, שאולי קורא להן עם מפתח `anon` בכוונה. **חסימה עיוורת עלולה לשבור את הווידג'ט**, ולכן לא נגעתי.

**מה לעשות:** לאשר לי גישת SQL ואבדוק את הגוף שלהן תוך דקות. אם אין בהן בדיקת בעלות:

```sql
-- אם הווידג'ט לא משתמש בהן:
revoke execute on function public.get_widget_coupons(integer) from anon;
revoke execute on function public.get_widget_statistics(integer) from anon;

-- אם כן: להוסיף בתוך הפונקציה בדיקה
-- if p_user_id <> app_user_id() then raise exception 'forbidden'; end if;
```

**ב. חמש טבלאות עם RLS דלוק ובלי אף מדיניות:** `user_ratings`, `user_reviews`, `push_system_config`, `telegram_users_audit_log`, `alembic_version`.

זה **לא חור אבטחה** — מצב כזה חוסם את כולם. אבל זה אומר שאחת משתיים: הפיצ'ר שבור, או שמשהו ניגש אליהן עם service role ועוקף. צריך להחליט לכל טבלה: מדיניות אמיתית, או להוציא אותה מסכימת ה-API.

### מפתח ההצפנה של הקופונים חשוף

`src/lib/encryption.ts:17` — קודי הקופונים מוצפנים, אבל המפתח כתוב קשיח בקוד ונשלח בתוך האפליקציה לכל מכשיר. כל מי שמוריד את קובץ האפליקציה מחלץ אותו.

**משמעות עסקית:** אם דלף גיבוי של מסד הנתונים, ההצפנה לא תעכב את התוקף. היא מגנה מפני הצצה מקרית, לא מפני תוקף.

**מה לעשות:** הפתרון האמיתי הוא הצפנה/פענוח בצד השרת (Edge Function), כדי שהמפתח לא יעזוב את השרת. זה שינוי ארכיטקטוני שדורש מיגרציה של הנתונים הקיימים — לא חוסם השקה, אבל חייב להיות רשום כסיכון ידוע.

### תשתית — שתי לחיצות בקונסולת Supabase

- **פוסטגרס עם עדכוני אבטחה ממתינים** (גרסה `15.6.1.132`). שדרוג מהקונסולה.
- **הגנה מפני סיסמאות דלופות כבויה.** הפעלה בקליק אחד — בודק סיסמאות מול HaveIBeenPwned.

---

## ✅ מה שתוקן בסבב הזה

### 17+12. נעילה אחרי ניסיונות התחברות כושלים

`legacy-login` היא פונקציה ציבורית בכוונה (היא זו שיוצרת את הסשן), אבל לא היה בה שום מונה ניסיונות — אפשר היה לנחש סיסמאות במהירות בקשות. הגבלות הקצב של Supabase Auth לא חלות על הנתיב הזה.

- טבלה חדשה `auth_login_attempts` — מיגרציה `supabase/migrations/0014_login_attempt_throttle.sql`
- **8 ניסיונות כושלים לכתובת מייל בחלון של 15 דקות**, ואז חסימה עם קוד 429
- הטבלה נכתבת רק עם service role. RLS דלוק בלי מדיניות = אף לקוח לא נוגע בה
- **מתנהג "fail open"**: אם הטבלה לא נגישה, ההתחברות ממשיכה לעבוד. מנגנון שיכול לנעול את כל המשתמשים בגלל תקלת תשתית גרוע מהסיכון שהוא מונע

> ⚠️ **המיגרציה טרם הורצה על מסד הנתונים.** עד שתרוץ, ההגבלה פשוט לא פועלת (הקוד ממשיך לעבוד כרגיל). להרצה: `supabase db push` או הדבקה בקונסולה.

### 10. תקרת שימוש ב-AI

`parse-coupon` קוראת ל-OpenAI על חשבונך. היא כבר רשמה צריכה לטבלת `gpt_usage` — אבל אף פעם לא קראה ממנה, כלומר לא הייתה תקרה. משתמש אחד יכול היה לשרוף את התקציב.

- **60 פענוחים למשתמש ב-24 שעות מתגלגלות**, ואז 429
- גם כאן fail open

### 11. הגבלת גודל בקשה

אותה פונקציה קיבלה תמונה בכל גודל:

- תקרת `Content-Length` של 8MB, נבדקת **לפני** קריאת הגוף
- תקרה על אורך הטקסט (20,000 תווים) ועל ה-base64 של התמונה
- ולידציה שה-base64 הוא באמת base64 (סעיף 6 חלקית)

---

## 🟡 פתוח להחלטה

### 14. CORS

`supabase/functions/_shared/cors.ts` מחזיר `Access-Control-Allow-Origin: '*'` לכל הפונקציות. באפליקציית מובייל זה כמעט חסר משמעות — אין דפדפן ואין עוגיות שנשלחות אוטומטית, וכל פונקציה מאמתת טוקן בעצמה. אם יש או יהיה ממשק ווב, כדאי לצמצם לדומיין שלך. **לא שיניתי** כי הידוק עלול לשבור לקוחות קיימים שאיני מכיר.

### 9. Prompt injection

טקסט חופשי מהמשתמש נכנס ישירות לפרומפט של `parse-coupon`. במקרה הגרוע, משתמש גורם למודל להחזיר ערכים שגויים **בקופון של עצמו**. אין דליפה בין משתמשים ואין הרחבת הרשאות. תקרת השימוש והגודל שנוספו מכסות את הנזק הכלכלי, שהיה החלק המשמעותי.

### 18. לוג אירועי אבטחה

קיימים `user_activities` ו-`task_execution_logs`. מעכשיו גם `auth_login_attempts` נותנת תיעוד מלא של כשלי התחברות. אין לוג ייעודי מרוכז לאירועי אבטחה — סביר לגודל הנוכחי.

---

## ✅ מה שכבר היה תקין

- **1. HSTS** — Supabase ו-Vercel אוכפים HTTPS ברמת הפלטפורמה.
- **4. תפוגת קישורי איפוס** — ברירת המחדל של Supabase (שעה).
- **5. מניעת זיהוי משתמשים** — מטופל במפורש ובמודע: `legacy-login` מחזירה הודעת שגיאה זהה למייל לא קיים ולסיסמה שגויה, עם הערה בקוד שמסבירה למה.
- **13. ניקוי קלט** — כל הגישה למסד דרך Supabase client עם שאילתות פרמטריות, אין שרשור SQL. אין רינדור HTML מקלט משתמש (רכיבי React Native), כלומר אין משטח XSS.
- **16. נתיבי אדמין** — מסך האדמין מוגן ב-`is_app_admin()` **ברמת מסד הנתונים**, לא רק בהסתרת כפתור. גם מי שיגיע למסך לא יקבל נתונים.
- **`verify_jwt` מתועד לכל פונקציה** — `supabase/config.toml` מציין במפורש לכל Edge Function אם היא דורשת טוקן, ולכל `verify_jwt = false` יש הערה שמסבירה מה מאמת את הקורא במקום. זה נוהג טוב מעל הממוצע.

---

## סדר פעולות מומלץ

1. **לאמת ולסגור את פונקציות ה-RPC האנונימיות** — הסיכון היחיד שהוא פוטנציאל דליפת נתונים בין משתמשים. בדיקה של דקות.
2. **להריץ את מיגרציה 0014** — בלעדיה הגבלת ההתחברות לא פעילה.
3. **לשדרג פוסטגרס + להפעיל הגנת סיסמאות דלופות** — שתי לחיצות.
4. **להחליט על חמש הטבלאות ללא מדיניות.**
5. **לתכנן העברת ההצפנה לצד שרת** — לא חוסם השקה, אבל לא להשאיר בלי בעלים.
