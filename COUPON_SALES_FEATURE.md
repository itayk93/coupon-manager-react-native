# מכירת קופונים — אפיון וארכיטקטורה

## הצורך העסקי

משתמש יכול למכור קופון בשתי דרכים:

1. לסמן קופון שנמכר מחוץ לאפליקציה.
2. למכור קופון כחלק מהעברת בעלות למשתמש אחר באפליקציה.

המוכר צריך לראות היסטוריית מכירות, פרטי קונה, הכנסות ורווח. קופון שנמכר לא צריך להשפיע על הארנק, היתרה, החיסכון, ההתראות, הווידג׳ט או יתר התצוגות הרגילות.

## ההחלטות וההתלבטויות

### טבלה חדשה מול עמודות בטבלת הקופון

הבקשה המקורית כללה סטטוס „נמכר” ופרטי מכירה. עלתה התלבטות אם להוסיף את כל הנתונים לטבלת `coupon`. הוחלט ליצור טבלת `coupon_sales` עצמאית ולהוסיף לקופון רק `sale_id`.

הסיבה: לאחר העברת בעלות אותה רשומת קופון שייכת לקונה. פרטי המכירה הם היסטוריה של המוכר, לא מאפיין שוטף של הקופון אצל הקונה.

### סטטוס „נמכר” מול קופון פעיל אצל הקונה

במכירה ידנית הקופון נשאר כרשומה היסטורית אצל המוכר ומקבל סטטוס `נמכר`.

בהעברה באפליקציה הקופון עובר לקונה. לכן סטטוס הקופון חוזר ל־`פעיל` או `נוצל`, לפי היתרה. סטטוס המכירה נשמר בנפרד כ־`completed`. כך המוכר רואה „נמכר”, והקונה מקבל קופון שמיש.

### שמירת רווח

לא נשמרת עמודת רווח. הרווח מחושב תמיד כך:

`sale_price - coupon_cost_snapshot`

נשמרים שווי, עלות ויתרה מנוצלת כפי שהיו בזמן המכירה. שינוי עתידי בקופון לא משנה דוחות עבר.

## מודל הנתונים

`coupon_sales` שומרת:

- קופון, מוכר וקונה רשום אם קיים.
- סוג מכירה: `manual` או `transfer`.
- סטטוס עסקה: `pending`, `completed`, `declined`, `cancelled`.
- שם פרטי, שם משפחה, טלפון ואימייל אופציונלי של הקונה.
- מחיר מכירה.
- צילום שווי, עלות, סכום מנוצל, חברה, תיאור ותוקף.
- קישור להזמנת ההעברה.
- מועדי יצירה, עדכון והשלמה.

`coupon.sale_id` הוא הקישור היחיד שנוסף לטבלת הקופון. `coupon_shares.sale_id` מחבר הזמנת העברה למכירה הממתינה.

הקישור בקופון מצביע למכירה האחרונה. היסטוריית המכירות נשארת מלאה, ולכן קונה שקיבל בעלות יכול למכור בעתיד את אותו קופון מחדש.

## תהליכים

### מכירה ידנית

פעולת DB אטומית:

1. מאמתת בעלות וקלט.
2. יוצרת צילום מכירה מושלם.
3. מעדכנת את הקופון ל־`נמכר`.
4. מוציאה אותו מהווידג׳ט ומעדכון אוטומטי.
5. מבטלת שיתופים קיימים.

### מכירה והעברת בעלות

יצירת ההזמנה יוצרת מכירה `pending`. רק אישור המקבל משלים באותה טרנזקציה את העברת הבעלות ואת המכירה. דחייה או ביטול משנים את המכירה בהתאם. מכירה ממתינה לא נספרת בסטטיסטיקה.

## ממשק

- „סמן כנמכר” במסך הקופון.
- „מכירה והעברה” ביצירת קישור ובשליחה לפי אימייל.
- „קופונים שמכרתי” באזור החשבון.
- „מכירת קופונים” בסטטיסטיקה: כמות, הכנסות, רווח ושווי.

## פרטיות ואבטחה

- הלקוח לא כותב ישירות לטבלת המכירות.
- מכירה ידנית והשלמת העברה מתבצעות בפונקציות DB מאובטחות ואטומיות.
- רק המוכר יכול לקרוא את רשומות המכירה שלו.
- קוד הקופון אינו נשמר בטבלת המכירות ואינו מוצג בהיסטוריה.
- המכירות נכללות בייצוא נתוני החשבון ובמחיקת החשבון.

## Trade-offs

- נשמר צילום כפול של חלק מפרטי הקופון. זה מגדיל מעט נפח, אך מגן על אמינות היסטוריית המכירות.
- `sale_id` נשאר על קופון שהועבר. הוא אינו חושף מידע לקונה, כי הרשאות הטבלה מגבילות קריאה למוכר.
- קונה שאינו משתמש באפליקציה אינו מקבל רשומת קופון. הקופון נשמר כהיסטוריה אצל המוכר בסטטוס `נמכר`.
- העברה פתוחה בקישור מסתמכת על פרטי הקונה שהמוכר הזין. זה מתאים לתיעוד העסקי, גם אם המקבל בפועל נכנס מחשבון אחר.
