# UX Fixes — 2026-08-08

## 1. כפתור העתקת קוד קופון
- נוסף כפתור "העתק קוד" במודל הצגת קוד גדול (מעל ה-QR)
- מעתיק ללוח עם טוסט אישור ואייקון ✓
- קבצים: `CopyCodeButton.tsx` (חדש), `CouponActionModals.tsx`, `DashboardModals.tsx`, `index.css`

## 2. ביטול אוטופוקוס במודלים
- מודל "עדכון סכום שימוש" (בשני מקומות) ומודל "דיווח מהיר על שימוש" — המקלדת כבר לא קופצת אוטומטית בפתיחה
- קבצים: `CouponActionModals.tsx`, `DashboardModals.tsx`

## 3. תמונת פרופיל — לחיצה על העיגול
- הוסר כפתור "החלף תמונה" נפרד
- לחיצה על עיגול האווטאר פותחת בחירת קובץ
- Hover מציג overlay "החלף תמונה"
- קובץ: `Profile.tsx`

## 4. תיקון RTL בשדה מגדר (פרופיל)
- הוספת `dir="rtl"` ל-Select כדי שהטקסט והחץ יהיו מיושרים נכון
- קובץ: `Profile.tsx`

## 5. Dialog מעל Drawer (blur fix)
- Dialog z-index הועלה ל-60 (מעל Drawer שנשאר 50)
- פותר את הבעיה שלחיצה על קופון בתוך drawer חברה הציגה מסך blur במקום פרטי קופון
- קובץ: `dialog.tsx`

## 6. Header sticky במובייל
- תוקן overflow ב-dashboard-body במובייל כדי שה-header יישאר תמיד גלוי
- קובץ: `index.css`
